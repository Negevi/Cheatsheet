#include <stdlib.h>
#include <stdio.h>

#include <stdlib.h>
#include <stdio.h>

void main() {
    srand(time(NULL));
    int i, n, range = rand() % 48 + 2;
    printf("RANGE: %d" range);

    int vec[48];

    for(i = 0; i < range; i++) {

    }
}
